import React from 'react';
import { View, Text } from 'react-native';
import { styles } from '../../styles';

export const DevsHeader = () => (
  <View style={styles.header}>
    <Text style={styles.title}>Sobre os Desenvolvedores</Text>
  </View>
);